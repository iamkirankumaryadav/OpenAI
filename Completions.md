# Completions

- Generating new text that is similar to text which was used for training.
- In simple words the generated response to the user prompt is called completions.

### Natural Language Processing (NLP) Models
AI models that work with text
- Chat completions
- Text completions
- Code completions 
